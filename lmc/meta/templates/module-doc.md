<!--
模块文档模板。使用方式：
1. 复制为 lmc/modules/<模块名>.md，文件名与代码中的模块标识一致；
2. 删除所有 HTML 注释与示例提示，填入终态内容；
3. 未在代码中确认的内容不要写入，确需保留则用「> 未验证：」标注；
4. 与模块无关的小节直接删除，不要保留空标题；
5. 完成后在 lmc/modules/README.md 登记，并按更新规则第 8 节自查。
-->

# <模块名>

一句话说明该模块的定位、解决什么问题，以及它在系统中的位置。

## 概览

说明模块的输入与输出、上游调用者与下游被调用者、开启方式（volfile 中的
`type` 与关键 option），必要时给出一段最小 volfile 片段。

## 核心概念

列出读懂本模块必须先掌握的少量概念，每个概念一段，术语以
[术语表](../../overview/glossary.md) 为准。

## 数据结构

列出关键结构体、它们之间的关系与生命周期。每项给出 `路径:行号`（`符号名`）。

## 工作流程

按主线流程分小节描述。涉及多步协作时用 Mermaid 表达顺序，图后给出一句结论。

## 关键分支与边界条件

说明异常路径、降级路径、并发与锁、失败重试等容易出错的判断点，并指向对应代码。

## 并发与锁

说明数据结构由哪把锁保护、加锁粒度、是否存在「调用方必须持锁」的接口约定，以及原子计数的
使用范围。不涉及并发的小节直接删除。

## 配置项

| option | 取值 | 默认值 | 作用 | 定义位置 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 与其他模块的交互

说明它依赖谁、被谁依赖，以及交互的接口形式（FOP、notify 事件、RPC、共享存储格式）。
只写结论并链接到对应文档，不复制对方细节。

## 可观测性

说明日志、statedump、metrics、event 等排查手段与对应代码位置。

## 代码位置

| 内容 | 位置 |
| --- | --- |
| 模块注册 |  |
| 主要实现 |  |
| 头文件与私有结构 |  |

## 相关文档

-
